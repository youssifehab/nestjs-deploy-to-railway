export const connection: Conncetion = {
  CONNECTION_STRING: 'CONNECTION_STRING',
  DB: 'MYSQL',
  DB_NAME: 'Test',
};

export type Conncetion = {
  CONNECTION_STRING: string;
  DB: string;
  DB_NAME: string;
};
